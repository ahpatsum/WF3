# WF3

# Dépôt du projet WF3 — site web statique / PHP.

# Contenu principal : `bo`, `cv`, `lp`, `newbo`, `OldLanding` et ressources (css, js, images).

# Pour lier et pousser ce dépôt sur GitHub :

# ```bash
# git remote add origin git@github.com:<votre-utilisateur>/WF3.git
# git push -u origin master
# ```

# Remplacez `<votre-utilisateur>` par votre nom d'utilisateur GitHub.

# Notes:
# - Le dépôt contient des fichiers binaires et assets. Vérifiez la taille avant de publier si vous utilisez un compte avec quota.